using System;
using System.Drawing;
using CellsAutomate.Creatures;
using Creatures.Language.Commands.Interfaces;
using Creatures.Language.Executors;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Creaturestests.Creatures
{
    [TestClass()]
    public class BaseCreatureTests
    {
    //    [TestMethod()]
    //    public void SetPositionTest()
    //    {
    //        var newPoint = new Point(0, 1);
    //        var creature = new Creature(new Point(0, 0), new Executor(), new ICommand[0], new Random(),  1);
    //        creature.SetPosition(newPoint);
    //        Assert.AreEqual(newPoint, creature.GetPosition);
    //    }
    }
}